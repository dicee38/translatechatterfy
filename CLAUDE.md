# Chatterfy AI Translator — расширение для Chrome (Manifest V3)

Отдельное, самостоятельное расширение — **не часть** `Chat Summarizer`
(другой проект, другая зона ответственности и чувствительность
данных: там статистика по лидам, здесь — личная переписка с живыми
людьми).

Помогает оператору вести переписку на диалекте собеседника: перевод
по выделению текста, расшифровка+перевод голосовых, в перспективе —
подсказка ответа с подтверждением оператора перед отправкой.

**Полное ТЗ: `docs/TZ.md` — читай его перед началом любой задачи.**
Там термины, архитектура целиком, схема API `messages/v1/search`
(данные берутся из него, не из DOM — см. п.0.1), спецификация
бэкенда-прокси, все три фичи с UI-состояниями и формат манифеста.

## Архитектура (не менять без явной просьбы)

```
content script (v2.chatterfy.ai) → свой бэкенд-прокси → OpenAI (транскрипция, перевод, умный ответ)
```

- **Данные — из API, не из DOM.** `POST /api/messages/v1/search`
  (`{chat_id, limit}`) отдаёт всю историю чата: `content` (текст),
  `sender_type: "incoming"/"outcoming"` (собеседник/оператор),
  `files[]` с `type: "voice"`, прямым `url` на `.ogg` и
  `meta.duration` — этого достаточно и для контекста диалекта, и для
  списка голосовых. DOM парсится **только** для позиционирования
  кнопок/плашек рядом с нужным сообщением, не для самих данных.
- **Ключ API никогда не в расширении.** Content script стучится в
  свой бэкенд-прокси (Cloudflare Workers или VPS — см. `docs/TZ.md`
  п.3.5), прокси держит ключ OpenAI и вызывает его сам. Один вендор на
  всё (транскрипция, перевод, умный ответ) — один ключ, не два.
- **Прокси обязателен: авторизация + бюджетный лимит.** Без токена на
  запросах к прокси и без дневного/месячного лимита по стоимости —
  прямой риск слить весь баланс API. Оба пункта — не опция, часть
  MVP с первого этапа (см. `docs/TZ.md` п.3.2–3.3).
- **Прокси не хранит контент дольше самого запроса.** Это переписка
  с реальными людьми — логировать текст/расшифровки на бэкенде
  дольше, чем нужно для ответа, не надо (см. `docs/TZ.md` п.3.4, 8).
- Три фичи — **read-only помощь**, ничего не отправляется и не
  меняется в Chatterfy автоматически. У Feature 3 (умный ответ) это
  отдельно закреплено архитектурно: вставка предложенного текста в
  поле ввода — только по явному клику оператора «Вставить», без
  автоотправки, это не ограничение MVP, а постоянное решение.
- **Поле ввода сообщения — Quill (`contenteditable`), не
  `<input>`/`<textarea>`.** Для Feature 3 вставка текста — через
  `document.execCommand('insertText', ...)`, не через React-инпут
  трюки (см. `docs/TZ.md` п.6.2).
- `manifest.json` — узкий `host_permissions` (конкретные домены
  Chatterfy + свой прокси), не `<all_urls>` как у Chat Summarizer —
  этому расширению не нужен доступ ко всему интернету.

## Чек-лист по этапам (docs/TZ.md, раздел 9)

- [x] **Этап 1 — качество хорошее, Feature 2 можно делать как задумано.**
      Ручная проверка `gpt-transcribe` на 9 реальных входящих голосовых
      (марокканская дарижа) через `scripts/stt-quality-check.js` на
      проде (не MOCK_MODE) — оператор прослушал и оценил транскрипты
      как качественные. По пути найдены и исправлены два реальных бага
      `/transcribe` (`response_format`, имя файла без расширения — см.
      `docs/TZ.md` §5.2) и откалибрована цена транскрипции по
      фактическому расходу ($0.05/мин, не $0.006). Транскрипты — в
      `docs/stt-quality-check-results.md` (не в git, реальная переписка).
- [x] **Этап 2 (скелет для обоих вариантов деплоя, сам деплой не сделан).**
      Бэкенд-прокси в двух версиях с одинаковым контрактом
      `/translate`/`/transcribe`, авторизация по токену, бюджетный
      лимит ($5/день по умолчанию, 429 при превышении). Один вендор на
      всё (транскрипция, перевод, умный ответ) — один ключ
      OPENAI_API_KEY, Claude/Anthropic не используется.
      - `proxy/` — обычный Node/Express, под VPS. Curl-проверен,
        покрыт `npm run test:skeleton` (MOCK_MODE=true).
      - `proxy/workers/` — под Cloudflare Workers (бесплатный тариф),
        KV вместо файла для счётчика, нативный `request.formData()`
        вместо `multer`. Проверен и логическим тестом
        (`npm run test:skeleton`, фейковый KV), и вживую через
        `wrangler dev` на настоящем Workers-рантайме (Miniflare) —
        `/health`, `/translate`, `/transcribe`, авторизация и рост
        бюджетного счётчика отработали.
      - **`proxy/workers/` задеплоен, живой, MOCK_MODE=false —
        реальные вызовы к OpenAI работают.**
        `https://chatterfy-translator-proxy.demonivan09.workers.dev`
        (секреты EXTENSION_TOKEN/OPENAI_API_KEY заданы через
        `wrangler secret put`, KV namespace `BUDGET_KV` создан,
        `workers.dev`-поддомен `demonivan09` зарегистрирован). Домен
        добавлен в `host_permissions` расширения
        (`extension/manifest.json`). Реальный `/translate` прогнан на
        проде — модель `gpt-4.1-mini`, перевод пришёл на диалекте и
        латиницей (арабизи), как в примере из HAR (TZ п.0.1), не
        литературной арабицей — промпт с системой письма работает как
        задумано. Фактическая стоимость одного вызова — $0.0001,
        списалась в KV-счётчик бюджета корректно (не оценка "до
        вызова", а посчитано по реальному `usage` из ответа OpenAI,
        см. `computeChatCostUsdFromUsage` в `pricing.js`).
        `/transcribe` с реальным ключом НЕ прогонялся — нет настоящего
        голосового файла под рукой, да и рано: этап 1 (ручная проверка
        качества STT) всё ещё не пройден, синтетический шум ничего не
        скажет о качестве распознавания диалекта.
        VPS-вариант (`proxy/`) не деплоился и не тестировался с
        реальным ключом (код обновлён параллельно, для консистентности,
        но не задеплоен) — не нужно, раз Workers-вариант уже поднят и
        рабочий.
- [x] **Этап 3 — проверено живьём на v2.chatterfy.ai, работает.**
      Content script (`extension/`): выделение текста, плашка с 4
      UI-состояниями, контекст диалекта через `messages/v1/search`,
      кэш — DOM-селектор контейнера чата (угаданный вслепую) подошёл
      с первой попытки, `messages/v1/search` реально отдаёт ожидаемую
      форму ответа.
      **Направление перевода теперь автоопределяется** (прошло два
      захода на живой проверке, см. `docs/TZ.md` §4.1 за полной
      историей). Итоговая логика — `detectDirection` в
      `extension/content.js`: смотрим не на `sender_type`, а на
      алфавит самого выделенного текста — заметная доля кириллицы →
      текст оператора → перевод в диалект (`direction: 'to_dialect'`,
      TZ п.4.2, дефолт); иначе (латиница/арабица) → текст уже на
      диалекте → перевод на русский
      (`direction: 'to_operator_language'`). Первая версия определяла
      направление по `sender_type` выделенного сообщения — сломалась,
      когда оператор отправлял уже переведённый на диалект ответ (он
      лежит в истории как `outcoming`, но фактически не на русском).
      Прогнано на проде (не в моке) реальными вызовами — оба
      направления подтверждены на примере из TZ п.0.1 (марокканская
      дарижа латиницей/арабизи) и на реальном репорте от оператора
      (уже отправленный дарижа-ответ корректно переводится на
      русский, а не повторно "в диалект").
      **Контекст диалекта (`messages/v1/search`) — три независимых
      бага подряд, все найдены и исправлены на реальном трафике** (не
      гаданием): неверный путь в ответе (`data.data.items`, не
      `data`/`data.items`), неверный порядок сортировки (API отдаёт
      от новых к старым, код брал не тот конец массива), отсутствие
      заголовка авторизации (`authorization: <JWT>` без `Bearer`,
      токен ищется в `localStorage`/`sessionStorage` по виду — см.
      `docs/chatterfy-api-reference.md` и `docs/TZ.md` §0.1.1 за
      полной историей). Подтверждено на живом чате оператором:
      контекст наполняется, перевод идёт в реальный диалект.
      Feature 1 — **полностью рабочая на проде**, дальше без явного
      запроса не трогаем.
      **Добавлен второй триггер по запросу оператора** (не новая фича,
      альтернативный UI для того же Feature 1): кнопка прямо в поле
      ввода Quill (`extension/compose-translate.js`, docs/TZ.md §4.4,
      в духе DeepL) — переводит набранный текст и заменяет его на
      месте, направление всегда `to_dialect`. Позиция кнопки —
      плавающая в углу поля, не встроена в панель инструментов
      Chatterfy — точную вёрстку их тулбара живьём не смотрели.
- [x] **Этап 4 (код готов, живьём на реальной DOM-вёрстке не проверен).**
      Feature 2 целиком:
      - `extension/chatterfy-api.js` — общая логика работы с Chatterfy
        API (getChatId/fetchChatMessages/buildDialectContext/JWT),
        вынесена из `content.js` в отдельный shared-модуль, чтобы
        Feature 1 и Feature 2 не дублировали трижды исправленную
        логику (см. запись про Feature 1 выше).
      - `extension/voice-translate.js` — список голосовых через
        `messages/v1/search` (`files[].type === "voice"`, только
        входящие — по той же причине, что и direction-фикс в Feature 1:
        исходящие голосовые часто оказываются заготовленными шаблонами
        оператора на русском), поиск `<audio>`-элемента в DOM по
        точному совпадению `src` с `files[].url` (не по угаданным
        CSS-классам — надёжнее к вёрстке), кнопка «Перевести» с оценкой
        стоимости по длительности, персистентный кэш в
        `chrome.storage.local` по `chatId:messageId` (реентерабельно —
        повтор после упавшего перевода не платит за расшифровку заново),
        5 UI-состояний (загрузка/успех/сетевая ошибка/бюджет исчерпан/
        «не дало разборчивого текста»).
      - `extension/background.js` — обработчик `transcribe`: скачивает
        голосовое (домен S3 добавлен в `host_permissions`), шлёт в
        прокси `/transcribe` с именем файла, содержащим правильное
        расширение (см. баг из этапа 1).
      - Попап — переключатель Feature 2 (`extension/popup.html/js`).
      - Прогон против реальных данных из этапа 1 (`gpt-transcribe` уже
        проверен на 9 голосовых) — сам пайплайн `/transcribe` рабочий.
        Не проверено: поиск `<audio>`-элемента на реальной странице
        (сопоставление по `src` — рабочая гипотеза, не подтверждена
        DOM-инспекцией живого чата в этой сессии).
- [x] **Этап 5.** Полировка — попап (`extension/popup.html`,
      `popup.js`): вкл/выкл Feature 1, ручной override диалекта
      (`targetLang` в `/translate`), настройка адреса прокси/токена
      (`chrome.storage.local`, общие дефолты в
      `extension/shared-defaults.js`), индикатор дневного бюджета
      через `GET /health`. Feature 2/3 переключателей нет — самих
      фич ещё нет, добавлять на их собственных этапах. Не требовало
      ключей моделей, проверено логикой против локального
      MOCK_MODE-прокси (`/health` отдаёт форму, которую ждёт
      `popup.js`); визуально в браузере не открывал — нет браузера в
      этой сессии.
- [ ] **Этап 6 (перспектива).** Feature 3 — контекст диалога,
      генерация ответ+обратный перевод, вставка в Quill-поле через
      `execCommand`, тест-кейс на отсутствие случайной автоотправки.

## Как работать

Ссылайтесь на конкретный этап в промпте, например:

```
Реализуй этап 2 из docs/TZ.md: скелет бэкенда-прокси с /translate и /transcribe
```

Claude Code сам откроет `docs/TZ.md`, найдёт нужный раздел (архитектуру,
формат эндпоинтов, промпты) и будет держать в контексте правила из
этого файла на протяжении всей сессии.
